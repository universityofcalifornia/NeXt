<nav>
  <ul class="nav navbar-nav">
    <li><%= link_to 'People', users_url %></li>
    <li><%= link_to 'Ideas', ideas_url %></li>
    <li><%= link_to 'Projects', projects_url %></li>
    <li><a href="<%= current_user ? logged_in_events_url : events_url %>">Events</a></li>
    <li><%=link_to 'About', "#" %></li>
    <li>
      <div>
        <% if current_user %>
          <%= link_to_if current_user, 'Groups', groups_path %>
          <span id="group_count">
            <%= render 'user_groups/count', :current_user => current_user %>
          </span>
        <% end %>
      </div>
    </li>
  </ul>
</nav>
